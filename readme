to start the back-end side use:
cd back-end
npm run dev
(must have mongodb, I used docker with mongodb)

to start the front-end side use
cd front-end
npm start

next steps are to move the server to the cloud (Cloud MongoDB), and build the application for the web, for the live preview.